import { CarouselTwoDirective } from './carousel-two.directive';

describe('CarouselTwoDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselTwoDirective();
    expect(directive).toBeTruthy();
  });
});
