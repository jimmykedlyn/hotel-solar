import { CarouselOneDirective } from './carousel-one.directive';

describe('CarouselOneDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselOneDirective();
    expect(directive).toBeTruthy();
  });
});
